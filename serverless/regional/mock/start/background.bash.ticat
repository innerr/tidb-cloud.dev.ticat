help = start local mock serverless-regional-service in background, verify it is accessable
abbr = bg
trivial = 1
flow = serverless.region.mock.start %delay=1s %env=apply

[arg]
verify-timeout-secs|timeout = 60

[dep]
curl = to verify http server is started
