help = start local mock-core-svc in background, verify it is accessable
abbr = bg

trivial = 1
unbreak-file-n-flow = true

flow = core-svc.mock.start %delay=0s %env=apply

[arg]
verify-timeout-secs|timeout = 60

[dep]
curl = to verify grpc server is started
