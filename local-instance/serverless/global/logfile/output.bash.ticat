help = if tidb cloud is running on local, print logs of serverless-global-service
abbr = cat|out

pack-subflow = true
quiet-err = true

flow = local-instance.serverless.global.log

[arg]
local-instance-start-cmd|start-cmd|cmd = lc.up
