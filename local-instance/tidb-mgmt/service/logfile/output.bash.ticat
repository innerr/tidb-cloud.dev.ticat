help = if tidb cloud is running on local, print logs of tidb-mgmt-service
abbr = cat|out
pack-subflow = true
flow = local-instance.tidb-mgmt.service.log

[arg]
local-instance-start-cmd|start-cmd|cmd = lc.up
