help = if tidb cloud is running on local, print logs of mock-core-svc
abbr = cat|out

pack-subflow = true
quiet-err = true

flow = local-instance.core-svc.mock.log

[arg]
local-instance-start-cmd|start-cmd|cmd = lc.up
